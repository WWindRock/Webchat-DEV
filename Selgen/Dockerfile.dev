# Selgen 开发环境 Dockerfile（修复版）

FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖（包括 concurrently）
RUN npm install

# 复制源码
COPY . .

# 暴露端口
EXPOSE 7000

# 开发模式启动（直接运行前端，跳过后端）
CMD ["npm", "run", "dev:frontend"]
